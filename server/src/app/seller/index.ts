import {types} from "./types";
import { muataion } from "./mutations";
import { resolvers } from "./resolvers";
import { queries } from "./queries";

export const Seller={types,muataion,resolvers,queries}